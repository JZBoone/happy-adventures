{
  "name": "happy-adventures",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint .",
    "start": "NODE_ENV=development npx nodemon --ignore ./src/js --ext .js ./src/server.js -p 3000",
    "build": "npm run build:dev",
    "build:dev": "NODE_ENV=development node build",
    "build:prod": "NODE_ENV=production node build",
    "deploy": "npm run build:prod && gsutil -m cp -r ./dist/* gs://happy-adventures",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.3",
    "dotenv": "^16.3.1",
    "esbuild": "^0.19.8",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "phaser": "^3.70.0",
    "prettier": "^3.1.0",
    "rxjs": "^7.8.1",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.13.2",
    "@typescript-eslint/parser": "^6.13.2",
    "eslint": "^8.55.0"
  }
}
